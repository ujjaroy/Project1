trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - '*'

pool:
  name: 'Self Pool'

steps:
- checkout: self

- script: |
    git remote rm origin
    git remote -v
    git remote set-url main https://ujjalkrroy0295@dev.azure.com/ujjalkrroy0295/MyProject/_git/AZ_Repo
    git push -u main --all
  displayName: 'Transfer code to Azure DevOps'